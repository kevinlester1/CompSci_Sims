Firebase rules for this workspace:

- Firestore rules are stored in firestore.rules.
- Deploy them from the Firebase console or with the Firebase CLI.
- The rules allow authenticated users to delete their own task and profile documents, and to manage their leaderboard entries.
